# Normalize all text files to LF in the repo (prevents CRLF from
# marking files as modified on Windows and triggering merge conflicts)
* text=auto eol=lf

# Explicitly mark shell scripts as LF
*.sh text eol=lf

# Binaries — no line ending conversion
*.png binary
*.jpg binary
*.ico binary
